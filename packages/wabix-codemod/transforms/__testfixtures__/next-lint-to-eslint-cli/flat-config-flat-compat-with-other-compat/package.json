{
  "scripts": {
    "lint": "wabix lint --fix --dir src --dir pages",
    "lint:strict": "wabix lint --strict",
    "lint:ci": "wabix lint --quiet --output-file lint-results.json",
    "precommit": "wabix lint --fix && npm test",
    "test": "jest && next lint",
    "complex": "npm run build && next lint --dir . --ext .js,.jsx,.ts,.tsx 2>/dev/null",
    "pipe": "wabix lint | tee lint.log",
    "redirect": "wabix lint > output.txt 2>&1",
    "multi": "wabix lint; next build; next lint --fix"
  },
  "dependencies": {
    "react": "^19",
    "react-dom": "^19",
    "wabix": "^16"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^8",
    "eslint-config-wabix": "^16"
  }
}
